runtime: &runtime
  seed: 42

  filter_by_cond: False
  num_observation: 1  # number of input reference images

dataset:
  <<: *runtime

transformer:
  <<: *runtime
  pretrained_model_name_or_path: THUDM/CogVideoX-2b

  modulate_encoder_hidden_states: True

train:
  <<: *runtime
  # output_dir: cirasim_bridge2_traj-image_320-480_finetune_2b_30k
  # output_dir: cirasim_droid_traj-image_384-256_finetune_2b_30k
  output_dir: cirasim_rt1_traj-image_320-480_finetune_2b_30k

  from_scratch: False  # train from scratch
  from_pretrained: True  # finetune from pretrained cogvideox

  learning_rate: 1.e-4
  lr_scheduler: cosine_with_restarts
  lr_num_cycles: 1
  lr_warmup_steps: 1000

  max_train_steps: 30000
  gradient_checkpointing: False

  # for 8 × A800 (80GB)
  train_batch_size: 4
  gradient_accumulation_steps: 2

  resume_from_checkpoint: NULL